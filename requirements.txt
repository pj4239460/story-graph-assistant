# Story Graph Assistant - Requirements

# Core
pydantic>=2.0.0
streamlit>=1.30.0
streamlit-flow-component>=1.6.1

# LLM
litellm>=1.0.0
python-dotenv>=1.0.0

# Vector Database & Embeddings (Optional - see KNOWN_ISSUES.md)
# ChromaDB 1.4.0 has stability issues on Windows, temporarily disabled
# Application uses keyword-based search as fallback
# To enable (unstable): pip install chromadb sentence-transformers torch --index-url https://download.pytorch.org/whl/cpu
# chromadb>=0.4.0
# sentence-transformers>=2.2.0
# torch>=2.0.0

# Optional (for future features)
# faiss-cpu>=1.7.4  # Vector store
# chromadb>=0.4.0   # Alternative vector store
# networkx>=3.0     # Graph algorithms
